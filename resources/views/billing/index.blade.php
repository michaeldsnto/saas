<x-app-layout>
    <x-slot name="header"><div><p class="text-sm uppercase tracking-[0.3em] text-cyan-300">Billing</p><h2 class="text-3xl font-semibold">Plans & Subscription</h2></div></x-slot>
    <div class="grid gap-6 lg:grid-cols-3">@foreach($plans as $plan)<form method="POST" action="{{ route('billing.checkout') }}" class="rounded-3xl border border-white/10 bg-white/5 p-6">@csrf<input type="hidden" name="plan_id" value="{{ $plan->id }}"><h3 class="text-2xl font-semibold">{{ $plan->name }}</h3><p class="mt-3 text-slate-400">{{ implode(', ', $plan->features ?? []) }}</p><p class="mt-4 text-4xl font-semibold">Rp {{ number_format($plan->price, 0, ',', '.') }}</p><div class="mt-4"><x-input-label for="gateway_{{ $plan->id }}" value="Payment Gateway" /><select id="gateway_{{ $plan->id }}" name="gateway" class="mt-1 block w-full rounded-2xl border-slate-700 bg-slate-950/70 text-slate-100"><option value="midtrans">Midtrans</option><option value="xendit">Xendit</option></select></div><x-primary-button class="mt-6">Choose Plan</x-primary-button></form>@endforeach</div>
    <div class="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]">
        <div class="rounded-3xl border border-white/10 bg-white/5 p-6"><h3 class="text-lg font-semibold">Current Subscription</h3><p class="mt-3 text-slate-300">{{ $subscription?->plan?->name ?? 'No active subscription' }}</p><p class="text-sm text-slate-400">Status: {{ $subscription?->status ?? '-' }}</p></div>
        <div class="rounded-3xl border border-white/10 bg-white/5 p-6"><h3 class="text-lg font-semibold">Billing History</h3><table class="mt-4 min-w-full text-left text-sm"><thead class="text-slate-400"><tr><th class="pb-3">Provider</th><th class="pb-3">Reference</th><th class="pb-3">Amount</th><th class="pb-3">Status</th></tr></thead><tbody class="divide-y divide-white/5">@foreach($payments as $payment)<tr><td class="py-3">{{ $payment->provider }}</td><td class="py-3">{{ $payment->provider_reference }}</td><td class="py-3">{{ $payment->amount }}</td><td class="py-3">{{ $payment->status }}</td></tr>@endforeach</tbody></table></div>
    </div>
</x-app-layout>
